import SwiftUI

// Placeholder — App Window feature comes later.
@available(macOS 15.0, *)
struct ContentView: View {
    var body: some View {
        Text("App Window kommt bald.")
            .frame(width: 700, height: 420)
    }
}
